spring:
  datasource:
    url: jdbc:h2:mem:rewards;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
    open-in-view: false
  sql:
    init:
      mode: always
